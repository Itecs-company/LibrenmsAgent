version: '3.8'

services:
  librenms-agent:
    container_name: librenms-agent
    build:
      context: .
      args:
        API_KEY: ${API_KEY:-}
    restart: unless-stopped
    ports:
      - "8888:80"
    environment:
      - TZ=UTC
